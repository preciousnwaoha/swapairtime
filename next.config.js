/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        // remotePatterns: [],
        domains: ["img.logoipsum.com", "ui-avatars.com" ]
    },
}

module.exports = nextConfig
